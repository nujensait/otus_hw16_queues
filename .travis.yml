# Required to run your project under the correct environment.
language: php

# Versions of PHP you want your project run with.
php:
  - 8.1

deploy:
  provider: pages
  skip_cleanup: true
  github_token: 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
  keep_history: true
  on:
    branch: main

sudo: required
services:
  - docker

env:
  COMPOSE_VERSION: 1.4.1

# Commands to be run before your environment runs.
before_script:
  - #cp .env.example .env
  - #cd code
  - #composer self-update
  - #composer install --prefer-source --no-interaction --dev
  - #cd ../

before_install:
 - echo "begin deploy"

script:
  - docker-compose down
  - #docker-compose build
  - docker-compose up -d
  - echo "Build done"

# Customize when the notification emails are sent.
notifications:
  email: true
  on_success: never
  on_failure: always

